import React from "react";

export function UnsubscribeForm() {
    return (
        <form action="">
            <legend>Unsubscribe Form</legend>
        </form>
    );
}

export default UnsubscribeForm;
