import React from 'react';
import FilterableGrid from "../components/FilterableGrid";

export const HOME_PAGE_ROUTE_PATH = "/";

export const HomePage = () => (
    <FilterableGrid />
);

export default HomePage;
